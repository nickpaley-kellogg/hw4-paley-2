<h1>New Place</h1>

<%= form_with url: "/places" do |form| %>
  <div class="mb-3">
    <%= form.label "name", class: "form-label" %>
    <%= form.text_field "name", class: "form-control" %>
  </div>
  <%= form.submit "Create Place", class: "btn btn-primary" %>
<% end %>